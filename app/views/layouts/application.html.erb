<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Fuel Reviews</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body style="background-color: #;">
  	
    <nav class="navbar navbar-expand-lg bg-dark mt-0" style="border:1px white solid;">

      <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>

      <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">

        <ul class="navbar-nav ml-auto" >

          <li class="nav-item" ><%= link_to image_tag("home.png" , width:30 , height:30), root_path%> </li> 
            <% if current_user.is_a?(GuestUser) %>
              <li class="nav-item mx-2 " > <%=link_to '<button type="button" class="btn btn-primary btn-sm"> Login </button>'.html_safe , new_user_session_path %></li>
              <li class="nav-item mx-2 " >  <%=link_to "Register",  new_user_registration_path%>  </li>
            <%else %>
             <li class="nav-item mx-2 " ><%= link_to "Logout", destroy_user_session_path, method: :delete%></li>
             <li class="nav-item" >  <%= link_to "User Profile", pages_UserProfile_path%></li>

          <%end%>
        </ul>
      </div>
    </nav>


    <%= yield %>
  </body>
</html>
